import { LoginComponent } from "@/components/login/LoginComponent";

export const metadata = {
  title: "Login",
  description: "Faça login em sua conta Comércios Locais",
};

export default function LoginPage() {
  return <LoginComponent />;
}
